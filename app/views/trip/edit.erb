<!DOCTYPE html>
<html>
  <head>
    <link rel = "stylesheet" type = "text/css" href = "/new.css">
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,400,600,700,800" rel="stylesheet">
  </head>

  <body class = "edit-page">
    <div class = "firstline">
      <h1 class = "firstline-title">Edit Trip</h1>
      <div class="dropdown">
        <button class="dropbtn">More</button>
        <div class="dropdown-content">
          <a href="/">Home</a>
          <a href="/trips-dashboard">Dashboard</a>
          <a href="/trips">Trips</a>
        </div>
      </div>
    </div>

    <form action="/trips/<%= @trip.id %>" method="post">
      <div class = "input-forms">
        <input type="hidden" name="_method" value="PUT" />

        <p><input type='date' name='trip[start_date]' value="<%= @trip.start_date %>"/> - <input type='date' name='trip[end_date]' value="<%= @trip.end_date %>"/></p>
        <p class = "input-describe">Duration(seconds):</p>
        <p><input type='integer' name='trip[duration]' value="<%= @trip.duration %>"/></p>
        <p class = "input-describe">Start Station Name:</p>
        <p><input type='text' name='trip[start_station_name]' value="<%= @trip.start_station_name %>"/></p>
        <p class = "input-describe">End Station Name:</p>
        <p><input type='text' name='trip[end_station_name]' value="<%= @trip.end_station_name %>"/></p>
        <p class = "input-describe">Bike ID:</p>
        <p><input type='integer' name='trip[bike_id]' value="<%= @trip.bike_id %>"/></p>
        <p class = "input-describe">Subscription:</p>
        <p><input type='text' name='trip[subscription_type]' value="<%= @trip.subscription_type %>"/></p>
      </div>
      <div class = "create-button">
        <p><input class = "button" type='submit' value="Edit"/></p>
      </div>
    </form>
  </body>
</html>
